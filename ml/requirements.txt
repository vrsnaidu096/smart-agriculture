# Sidecar runtime
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pillow>=10.0
numpy>=1.24

# Model backend. Installed only when ML_BACKEND=hf (the default for real use).
# On a CPU-only machine install torch from the CPU index for a much smaller download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
transformers>=4.40

# Harness only
scikit-learn>=1.4
tqdm>=4.66
